const Endpoints = {
  AUTH: {
    REGISTER: '/auth/register',
    LOGIN: '/auth/login',
    REFRESH: '/auth/refresh',
    LOGOUT: '/auth/logout'
  },

  RESOURCES: {
    PROFILE: '/profile'
  }
}

export default Endpoints